# 更新日志

本项目在 1.0.0 正式版之前均为开发版（`-dev`）。
版本号格式为 `主版本.次版本.修订号-dev`，遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

最新版本在最上方。

---

## v0.2.6-dev

### 新增

- **暂停回复**：生成过程中发送按钮变为红底白方块，点击即停。
  任何阶段都能暂停，包括还在连接服务器、尚未收到任何内容时。
  已经生成的内容会保留而非作废——暂停的本意是「到此为止」，
  不是「重来」。一个字都没收到时不留空消息
- **详细发送状态**：输入栏左下显示当前进行到哪一步（正在连接服务器 /
  正在等待模型响应 / 正在接收回复），替代笼统的「发送中」。
  这几个阶段的耗时特征不同，卡在哪一步有助于判断问题所在
- **用户头像展示**：抽屉顶部与自己的消息旁都会显示，
  此前设置了自定义头像却没有任何地方能看到。
  头像模式的默认值同时从「不显示」改为「内置图标」——原先的默认值
  会把两个显示开关全部拦住，用户改了设置也看不到变化
- **头像显示开关**：设置 → 聊天外观 → 头像，
  可分别控制用户与助手头像是否显示
- **消息长按菜单**：长按消息弹出操作表。助手消息可复制、重新生成、
  删除、查看信息；用户消息可复制、编辑并重发、回滚到此处、删除、查看信息。
  朗读、修改记忆、插入总结、创建分支依赖尚未落地的能力，暂未提供

### 变更

- **用户消息默认改为无气泡**，与助手一致。此前两侧形态不同看起来割裂，
  想要气泡可在设置里单独打开

### 修复

- **暂停后消息仍停在「发送中」**：改为主动结算，不再依赖取消异常
  穿过 flow、NonCancellable 与 Room 三层传播——任一环节没走到
  就会留下卡住的消息。打开会话时也会结算一次，
  顺带清理进程被杀留下的历史残留
- **用户头像仍不显示**：旧默认值 NONE 已写进本地存储，
  仅改默认值对已装设备无效。加了一次性迁移，用独立标记记录，
  之后「不显示」这个选项照常可用。
  迁移的触发点也从设置页移到了应用启动——放在设置页的话，
  没进过聊天外观页的用户永远等不到它执行
- **无气泡时用户消息靠左**：容器撑满了整行，文本自然从左边缘开始排。
  现在只有助手侧撑满（代码块与表格需要完整宽度），用户侧随内容宽度
- **两侧气泡形状不一致**：用户气泡的右下角此前收窄至 4dp 作为尾巴，
  现已与助手统一为四角圆角。来源方向由对齐与底色区分，
  不需要再靠形状表达

- **深色主题下部分图标看不见**：OpenAI、xAI、Anthropic 等 8 个单色图标
  用 `currentColor` 填充，SVG 独立加载时没有可继承的上下文颜色，
  会落到黑色。现在这些图标跟随主题的 `onSurface` 自动翻转。
  彩色图标不受影响，着色会破坏品牌识别度
- **助手消息下方多余的「发送中」**：改为只在用户侧显示。
  助手侧的进度已由输入栏承担，流式回复本身也在逐字呈现

- **更新日志排版**：版本号明显放大，各版本之间的分隔线加上留白。
  此前二级与三级标题只差 2sp，版本号与其下的分类标题混成一片

---

## v0.2.5-dev

### 新增

- **模型厂商图标**：按模型名自动匹配并显示厂商标识，抽屉底部与消息头像
  都会用到。匹配的是模型名而非提供商配置，因此中转服务的模型也能正确显示——
  `[限时福利]claude-opus-5-thinking` 会显示 Claude 图标。
  内置 29 个 SVG（约 56KB），不走网络
- **顶栏与输入栏透明度可配置**：设置 → 聊天外观 → 栏目透明度。
  顶栏默认透明（它没有独立内容，铺色会在背景图上切出一条色带），
  输入栏默认不透明（透上来的背景会影响输入文字的可读性）
- **应用内更新日志**：设置 → 关于 → 更新日志不再跳转 GitHub，
  内容由构建任务从仓库根目录同步进 assets，直接在应用内渲染

### 变更

- **抽屉底部的模型行改为直接进入提供商配置**：此前与「设置」行指向同一处，
  点模型只能到设置首页，还得再点一次「提供商」
- **提供商配置页改用新的设计语言**：此前仍在用描边输入框与分割线，
  现已统一为「分类标题 + 圆角分组容器」，与其余设置页一致
- **本地图片改用 Coil 加载**：原实现是手动 BitmapFactory 解码，
  每次进入页面都重新解码且无缓存。Coil 本就是项目依赖，
  此前判断「未引入图片库」有误

### 修复

- **抽屉里的助手头像显示方括号**：原本取模型名首字符，
  而中转常给模型名加 `[...]` 前缀。现在优先匹配厂商图标，
  匹配不到时取第一个字母或数字
- **更新日志读取失败时停在加载状态**：加载中与失败共用一个占位，
  真失败时表现为永久转圈。现在失败原因会显示在页面上方

---

## v0.2.4-dev

### 修复

- **无法选择头像与背景图**：解码时对同一个 URI 调用了两次
  `openInputStream`——第一次读尺寸信息，第二次实际解码。部分系统的
  content provider 不支持重复打开，或临时读权限在首次关闭后即失效，
  第二次读取拿到 null 导致保存失败。现改为一次性读入字节数组后解码两次
- **失败时没有任何提示**：`save()` 整体包在 catch 里并返回 null，
  调用方静默跳过，选完图界面毫无反应，无法判断是没选中还是出错。
  现在会区分原因（权限不足 / 格式不支持 / 内存不足 / 写入失败）并在页面顶部提示。
  另外补上了 `OutOfMemoryError` 的捕获——它是 Error 而非 Exception，
  原先的 `catch (e: Exception)` 抓不到
- **聊天外观页的容器重叠**：头像模式与图片选择、背景模式与背景预览
  各自是独立的圆角容器，中间没有分类标题分隔。`SettingsGroup` 自身
  不带外边距，因此两组紧贴在一起。现已补上间距

### 其他

- 降采样率计算抽为独立文件并补 6 项测试。这段算术决定大图能否被解码，
  算错会直接 OOM，而它原先在依赖 Android API 的文件里无法单测

---

## v0.2.3-dev

外观自定义。此前配色只有「跟随壁纸」与「品牌橙」两种，消息形态也固定为
用户有气泡、助手无气泡，本版把这些交给用户选择。

### 新增

- **六套预设配色**：品牌橙、静蓝、松绿、藤紫、玫红、石墨。
  在设置 → 外观 中选择，关闭动态取色后生效。
  各套由基准橙色板旋转色相派生，因此明暗层次与对比度表现一致
- **气泡样式可选**：设置 → 聊天外观，用户与助手消息各自可选「气泡」或「无气泡」。
  助手默认无气泡（代码块与表格可占满宽度），用户默认有气泡
- **消息头像**：可选不显示、内置图标或自定义图片。
  内置图标下助手显示当前提供商首字母，用户显示人形图标
- **聊天背景图**：可选择本地图片作为聊天背景，附遮罩浓度调节。
  背景图会降低文字对比度，遮罩用于补偿
- **首屏提示词可折行显示**：不再横向滚动

### 变更

- 设置页新增「聊天外观」入口，位于「对话」分类
- 选择的图片会缩放后复制到应用私有目录（头像长边 512、背景 1920），
  而非引用原文件——避免相册中的原图被删除或移动后头像消失

### 说明

- 动态取色开启时预设配色不生效，因为动态取色的整套色板来自壁纸，
  两者无法叠加。此时配色选择器会置灰
- 头像不支持自动获取各家提供商的 logo：这些服务没有稳定的公开 logo 地址，
  网络拉取不可靠。需要真实 logo 可自行上传图片

---

## v0.2.2-dev

### 修复

- **消息状态卡在「发送中」**：部分中转服务在回复结束后既不发送 `[DONE]`
  也不关闭 SSE 连接，读取会一直阻塞到 OkHttp 的 5 分钟读超时，
  期间用户消息状态不更新、token 用量与耗时也不写入。现已在收到首个数据块后
  改用 20 秒空闲超时，无新数据即视为流正常结束；同时收到 `[DONE]` 后主动退出，
  不再等待连接关闭
- **Claude 经部分中转服务时回复丢失**：这类服务会把 Claude 请求转成 OpenAI
  协议再转发，返回的 SSE 因而是 OpenAI 格式（正文在 `choices[].delta.content`，
  且没有 `type` 字段）。此前只按 Anthropic 格式解析，所有增量被跳过。
  现已同时兼容两种格式，流式与非流式皆可

### 变更

- **首屏提示词改为折行堆叠**：原先横向滚动，首尾 chip 被裁成半截，
  既看不出共有几条也不知道该往哪滑。现改为折行排列，全部可见

---

## v0.2.1-dev

v0.2.0-dev 的界面细节修正，均来自真机验证发现的问题。

### 修复

- **键盘弹出时布局错乱**：manifest 缺少 `windowSoftInputMode="adjustResize"`，
  配合 `enableEdgeToEdge()` 后窗口不重新布局，键盘弹出时整个界面被向上推——
  聊天内容顶进状态栏，输入框被键盘遮住一半。现已补上该属性并为聊天页加
  `imePadding()`，键盘弹出时输入框上移到键盘上方
- **输入框四周有一圈多余白边**：外边距原本加在 `Surface` 上，padding 位于容器内侧，
  容器本体被压缩后四周露出页面背景。因容器色与页面色相近，看起来像描边。
  现改为容器铺满宽度、只圆上方两角、贴住屏幕底边
- **用户消息的元信息左对齐**：气泡靠右而下方的时间戳、token 用量靠左，两者脱节。
  现已跟随气泡靠右

### 变更

- **问候语字号加大**：从 22sp 提到 34sp 并加重字重。首屏留白大，
  问候语是唯一主体元素，此前体量不足以撑住版面
- **首屏提示词支持每次随机显示**：内置候选从 4 条扩充到 12 条，
  默认每次进入空会话时随机抽 4 条。可在设置 → 首屏提示词中关闭，
  关闭后按配置顺序显示前 4 条。手动增删候选的功能保持不变

---

## v0.2.0-dev

界面重构版本。此前的界面基本停留在 Compose 模板默认样式，这一版重做了配色、
形状、排版与全部页面结构，并补上了消息元信息与请求日志两项诊断能力。

### 新增

- **会话抽屉**：从左侧滑出，按今天 / 昨天 / 本周 / 本月 / 更早分组，
  每条显示标题与末条消息摘要。长按重命名或删除
- **空会话首屏**：按时段显示问候语，下方是可横滑的提示词入口
- **消息元信息**：可在设置中开启时间戳、模型名、token 用量、生成速度、响应耗时。
  后四项依赖本版新增的数据字段，仅对新消息生效
- **请求日志**：设置 → 诊断 → 请求日志，记录最近 100 条接口请求的地址、
  状态码、耗时与响应内容，可展开查看请求体与响应体，用于排查发送失败的原因。
  API Key 在记录时即已脱敏；日志仅保存在本机内存，重启应用后清空
- **深色模式设置**：可选跟随系统 / 始终浅色 / 始终深色
- **动态取色开关**：Android 12 及以上可选择跟随壁纸取色或使用应用配色
- **首屏提示词可编辑**：新增、删除、恢复默认

### 变更

- **移除首页**：会话列表改由聊天页抽屉承载，打开应用直接进入对话
- **新会话延迟创建**：打开应用或点击「新对话」时不再立即写入数据库，
  首次发送消息才创建。此前每次启动都会留下一条空会话
- **助手消息改为全宽无容器**：此前限宽 340dp 并铺底色，代码块与表格被挤在窄条里。
  用户消息保留气泡，右下角收窄指示方向
- **输入区重做**：合并为单一圆角容器，功能行显示当前提供商
- **顶栏显示模型信息**：双行结构，第二行为「模型 · 提供商」
- **设置页改为两级结构**：一级页只做分类导航，分为通用、模型与服务、对话、
  诊断、关于五组；提供商配置等具体项移入子页面
- **配色重做**：以品牌橙 `#FCA43C` 为种子色建立完整 Material 3 色板，
  含 surface 五级色阶；深色模式使用暖调深灰而非纯黑
- **排版重做**：补全全部字号档位，行高统一为 1.4–1.5 倍，关闭默认行距补偿。
  仍使用系统字体——实测 Inter 等外部字体不含中文字形，混排时基线错落反而更差

### 修复

- **切换会话时消息串台**：`loadMessages` 每次调用都会新建订阅而不取消旧的，
  导致快速切换会话后两个会话的消息互相覆盖。改版前每个页面只调用一次因而未暴露，
  抽屉切换会话则会触发。现已用 Job 持有并在重新订阅前取消
- **连续点击发送按钮可能重复发送**：加载状态的设置此前在协程内部，
  存在时间窗口；现已提到协程外

### 数据

- 数据库升级至版本 3，消息表新增模型名、输入 / 输出 token 数与耗时四个字段。
  升级使用 `ALTER TABLE ADD COLUMN`，不重建表，历史对话完整保留。
  新增字段对旧消息为空，界面会跳过而不显示为 0
- OpenAI 流式请求新增 `stream_options.include_usage` 以索取 token 用量；
  Claude 从 `message_start` 与 `message_delta` 两个事件合并；Gemini 取 `usageMetadata`

---

## v0.1.4-dev

### 修复

- **模型列表无法获取**：`HttpTransport` 的 `getForText` 与 `postForText` 虽标记为 `suspend`，
  但内部使用 OkHttp 的阻塞式 `execute()` 且未切换线程，从主线程调用时会抛
  `NetworkOnMainThreadException`。现已用 `withContext(Dispatchers.IO)` 包裹。
  同一问题也会导致关闭流式输出后无法发送消息，一并修复（流式路径因已有
  `flowOn(Dispatchers.IO)` 而未受影响）
- 新增回归测试，断言两个同步请求方法保持 `suspend` 签名

### 变更

- **API Key 改为明文显示**：不再使用密码掩码，便于核对与修改
- **设置自动保存**：移除「保存设置」按钮，改动在停止输入约 0.6 秒后自动落盘；
  切换提供商与开关类改动立即保存；点击返回时会先刷盘，避免防抖窗口内的改动丢失
- 切换提供商前会先保存当前提供商的未落盘改动，切换后立即记录新的当前提供商

---

## v0.1.3-dev

### 新增

- **Markdown 渲染**：助手回复不再是纯文本，支持以下语法
  - 标题（`#` 到 `######`，按级别使用不同字号）
  - 围栏代码块，显示语言标签，提供复制按钮，长行横向滚动而非折行
  - 行内代码、加粗（`**text**`）、斜体（`*text*` / `_text_`）、删除线（`~~text~~`）
  - 链接（`[文本](地址)`），显示为主色带下划线
  - 无序列表（`-` / `*` / `+`）与有序列表（`1.` / `1)`），支持缩进层级
  - 引用块（`>`）与分割线（`---`）
- 自实现轻量解析器 `MarkdownParser`，未引入第三方 Markdown 库
- **多 AI 提供商支持**：可在设置中切换 OpenAI、Anthropic Claude、Google Gemini
  - OpenAI：`POST v1/chat/completions`，鉴权 `Authorization: Bearer`
  - Claude：`POST v1/messages`，鉴权 `x-api-key` 并附带 `anthropic-version: 2023-06-01`；system 提示提取到顶层 `system` 字段；`max_tokens` 为必填项，固定发送 4096
  - Gemini：`POST v1beta/models/{模型}:generateContent`，模型名写在 URL 路径而非请求体；消息结构为 `contents[].parts[].text`，助手角色映射为 `model`；system 提示走 `systemInstruction` 字段；鉴权使用 `x-goog-api-key` 请求头而非 `?key=` 查询参数，避免密钥出现在 URL 与日志中
- **各提供商配置独立存储**：API Key、模型名、API 地址按提供商分别保存，切换提供商时自动回填该提供商自己的配置，不会互相覆盖
- **模型列表动态获取**：设置页新增「获取模型列表」按钮，从各家接口实时拉取，不再使用应用内硬编码的模型名单
  - OpenAI 读 `GET v1/models`，Claude 读 `GET v1/models`，Gemini 读 `GET v1beta/models`
  - Gemini 结果会去掉 `models/` 前缀，并过滤掉不支持 `generateContent` 的模型（如嵌入模型）
  - 结果按名称排序后平铺展示在页面内，点击即填入
  - 结果缓存到本地，重进设置页或重启应用后列表仍在，只在主动点击刷新时重新拉取
  - 拉取使用输入框中的当前 Key 与地址，改完无需先保存即可验证
- 模型名输入框保持可自由输入，以支持中转服务提供的非官方模型名
- 设置页按当前提供商显示对应的 Key 格式示例与默认地址
- 新增 13 个 provider 协议单元测试，覆盖三家的鉴权头、请求体结构、SSE 事件解析与模型列表解析；新增 4 个 Repository 用例，覆盖按提供商分发客户端与缺失实现时的报错。测试总数由 9 个增至 22 个

### 修复

- 流式输出过程中未闭合的代码围栏现在仍按代码块渲染，不会在收到结尾围栏的瞬间整段样式跳变
- 幂运算 `2**3` 不再被误识别为加粗标记并把后续文本整段加粗
- `snake_case` 形式的标识符中的下划线不再被当作斜体标记
- `#标签` 这类井号后无空格的文本不再被误判为标题
- 未闭合的强调标记按普通字符显示，不会因流式中间态导致样式闪烁
- OpenAI 兼容响应中缺少 `id`、`index` 或 `usage` 字段时不再抛出反序列化异常导致整条回复丢失，现在只要存在正文即可正常取用（部分中转服务会省略这些字段）

### 变更

- 用户发送的消息保持纯文本显示，不解析 Markdown，避免输入的符号被意外改变样式
- 助手气泡最大宽度从 300dp 放宽到 340dp，减少代码块的横向滚动
- 新增 14 个 Markdown 解析单元测试
- **应用图标**：替换为新图标，同步生成 mdpi 至 xxxhdpi 各密度的传统图标、圆形图标与自适应图标前景层；自适应图标背景色由深色 `#0F131C` 改为取自图标的暖橙色 `#FCA43C`
- **网络层重构**：移除 Retrofit 依赖，改由 OkHttp 直接构造请求。原因是三家的请求路径、鉴权方式与响应结构差异过大，Retrofit 的接口声明方式反而增加了适配成本
- 新增统一的 `ChatClient` 接口与 `Transport` 传输抽象，各提供商实现自己的请求构造与响应解析；`ChatRepositoryImpl` 不再包含任何单一服务商的协议细节，只负责组装对话内容、选择当前提供商与处理失败状态
- 鉴权头注入与地址拼接从 OkHttp 拦截器移入各 provider 客户端
- 删除 `OpenAIApi`、`AuthInterceptor`、`ChatStreamSource`，职责已由 provider 层接管
- 默认地址与默认模型不再定义在 `Constants`，改由 `AiProvider` 按提供商给出

### 升级注意

- 原有的 OpenAI API Key、模型与地址会自动迁移到新的存储结构，无需重新填写
- 对话记录不受影响，数据库结构未变动（仍为版本 2）
- 首次使用 Claude 或 Gemini 需分别填写各自的 API Key

### 已知限制

- 尚无界面入口设置系统提示（system prompt），但协议层已支持，三家的处理方式均已实现
- Claude 的 `max_tokens` 固定为 4096，暂不可配置
- Gemini 使用 `v1beta` 接口版本，后续可能需跟随官方调整

---

## v0.1.2-dev

### 新增

- **流式响应（SSE）**：回复逐字显示，不必等待完整生成
  - 新增 `ChatStreamSource`，基于 OkHttp 逐行读取 `text/event-stream`
  - 助手消息先以空占位入库，随增量更新内容，界面通过 Room Flow 自动刷新
  - 设置中新增「流式输出」开关，默认开启；部分中转服务不支持 SSE 时可关闭退回一次性返回

### 修复

- OkHttp 读超时从 60 秒延长到 5 分钟，此前流式长连接会被提前掐断
- `AuthInterceptor` 此前只替换协议、主机和端口，若自定义 API 地址带路径前缀
  （如 `https://relay.example.com/openai/`），该前缀会丢失导致请求失败；现在会正确拼接
- kotlinx.serialization 的 `encodeDefaults` 此前为 `false`，导致请求体中的 `stream` 字段
  根本不会被发送，服务端始终按非流式返回
- 流式传输中断时会删除半截的助手占位消息，只将用户消息标记为失败并保留重试入口，
  不再留下无意义的残缺回复

### 变更

- `ChatStreamSource` 抽为接口，OkHttp 实现改名 `OkHttpChatStreamSource`，便于替换与测试
- CI 在测试失败时直接打印失败用例名与断言详情，不必再下载日志压缩包排查
- 新增 3 个流式相关单元测试（分片拼接、中断清理、开关生效）

---

## v0.1.1-dev

### 新增

- **多轮对话上下文**：请求会携带该会话的历史消息，最多保留最近 20 条以控制 token 消耗
- **自定义 API 地址**：设置中可填入中转服务地址，通过拦截器在运行时改写请求目标
- **发送失败重试**：消息新增 `sending` / `success` / `failed` 状态，失败的消息标红并显示
  失败原因与「重试」按钮
- **会话自动命名**：首条用户消息的前 20 个字符自动作为会话标题，超长截断并加省略号
- **APK 签名发布**：发行版改为只提供一个已签名的 `AfyzHub-vX.Y.Z-dev.apk`，可直接安装
- 项目补充 `.gitignore`，此前缺失，存在误提交 keystore 或构建产物的风险
- 引入 JUnit 与 kotlinx-coroutines-test，新增 9 个单元测试，CI 在构建前执行

### 修复

- **对话没有上下文**：此前每次请求只发送当前一条用户消息，多轮追问完全失效
- **设置中选择的模型不生效**：此前请求体的 `model` 硬编码为默认值，用户的选择被忽略
- **API Key 泄漏到系统日志**：`HttpLoggingInterceptor` 此前固定为 `BODY` 级别，
  会把 `Authorization` 头和完整对话内容写入 logcat；现在 release 构建下关闭日志
- **失败消息残留**：此前用户消息先入库，API 调用失败后这条消息会留在界面上且无法重试
- 修正聊天页标题的错别字「对觞」，应为「对话」
- 修正 `Constants.kt` 的缩进错乱

### 变更

- 新增 `SettingsRepository` 统一管理设置读写，并抽出只读接口 `SettingsProvider`
  供数据层依赖，便于在 JVM 上测试
- 拦截器改为从内存中的 `StateFlow` 快照同步读取 API Key，不再每次请求都用
  `runBlocking` 阻塞网络线程读磁盘
- 移除 `ChatRepository.sendMessage` 和 `SendMessageUseCase` 中从未被使用的 `apiKey` 参数
  （鉴权实际由拦截器注入）
- 模型选择由固定下拉框改为可自由输入，并保留常用模型快捷选项，以支持中转服务的非官方模型
- 数据库版本从 1 升到 2（消息表新增 `status`、`errorMessage` 字段和会话索引），
  提供迁移脚本而非破坏性重建，升级时保留历史对话
- Release 构建启用签名与混淆后体积从 12.87MB 降至约 2.4MB（此前发布的是 debug 包）

### 破坏性变更

- 从 v0.1.0-dev 升级需要先卸载。v0.1.0-dev 的安装包使用 debug 签名，
  与本版本的正式签名不匹配，系统会拒绝覆盖安装。卸载会清除本地对话数据。

---

## v0.1.0-dev

首个开发预览版，完成基础可用的聊天闭环。

### 新增

- 基于 Kotlin + Jetpack Compose 的聊天界面，遵循 Material Design 3
- OpenAI Chat Completions 接口集成（非流式）
- 会话列表与对话历史本地保存（Room）
- API Key 配置，保存在本机 DataStore
- Material You 动态主题
- GitHub Actions 自动构建并发布预发行版

### 技术栈

- Kotlin 2.0.20、AGP 8.7.0、Gradle 8.9
- Jetpack Compose（BOM 2024.10.00）、Navigation Compose
- Koin 4.0.0 依赖注入
- Retrofit 2.11.0 + OkHttp 4.12.0 + kotlinx.serialization
- Room 2.6.1、DataStore 1.1.1
- 最低支持 Android 8.0（API 26），目标 API 35

### 已知问题

> 以下问题在 v0.1.1-dev 中修复。

- 对话不携带上下文，每轮提问相互独立
- 设置中选择的模型不生效
- API 地址硬编码为官方域名，无法使用中转服务
- release 构建产物未签名，无法直接安装
